name: flutter_application_3
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  expressions: ^0.2.5+2
  sqflite: ^2.3.3+2 # Ensure this is the latest compatible version
  path: ^1.8.0
  http: ^1.2.2 # Ensure this is the latest compatible version
  audioplayers: ^6.1.0 # Latest as per your usage

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0 # Downgraded to avoid version conflict

flutter:
  uses-material-design: true

  assets:
    - android/assets/welcome_background.png
    - android/assets/about_screen.png
    - android/assets/home_screen.png
    - android/assets/Trivia.png
    - android/assets/Science.png
    - android/assets/Technology.png
    - android/assets/History.png
    - android/assets/congratulations.png
    - android/assets/WelcomeBg.png

  fonts:
    - family: Coiny
      fonts:
        - asset: fonts/Coiny/Coiny-Regular.ttf
